- ~~流式暂停：协议层（packages/protocol/src/rpc.ts / transport.ts）当前没有主动中断 RPC~~ → **已关闭（Phase 1）**：`RpcClient.interrupt(sessionId)` 已新增（`/api/session.interrupt`，与 `unary` 同构）；`Connection.interrupt?` 可选字段 + `LanTransport` 接线；聊天页暂停按钮先发中断，失败才回退本地暂停并提示。宿主侧（DSH）暂无 `session.interrupt` 实现时，`harness-plugin/src/interrupt.ts` 提供接线桩（入参校验 + 响应构造），待真机实现后接入。
- **TLS 部署实测（Caddy/Docker）**：Docker CLI 可用，但 Docker Desktop daemon 未启动（`npipe:////./pipe/dockerDesktopLinuxEngine` 不可达），且本机无 `caddy` 可执行文件。阻塞内容：按 `docs/MANUAL.md` 2.8 节用 Caddy 终止 TLS 并验证 WSS 连接。替代已做：relay 服务器 + WSS/WS 协议路径已被 relay 26+ 测试覆盖，TLS 仅缺反代层实机验证。下一步：启动 Docker Desktop 后执行 `docker run --rm -p 8443:8443 -v <Caddyfile>:/etc/caddy/Caddyfile caddy:latest` 并验证 `wss://localhost:8443` 握手。
- **Android 真机 UI 验证（R1 远程优先首屏 + relay 配对）**：电脑 `adb devices` 能识别设备（MXW-AN00 / UANVB20827000887，USB 与 `adb connect 192.168.1.21:5555` 无线均在线），`adb shell echo` 初始可通；但一旦通过 `adb shell am start -a android.intent.action.VIEW -d exp://...` 打开 Expo Go 后，`adb shell` / `adb devices` 全部无响应（USB 与无线一样），手机仍可 ping 通（192.168.1.21 在线）。Web 端替代已验证：Playwright（390×844）完成远程模式首屏 → 填 relay 地址 + 6 位配对码 → 配对成功 → 设置页/插件页截图（`.shots/relay-mode-*.png`、`.shots/relay-mode-find.txt`）。下一步：在手机上确认 Expo Go 是否卡在启动页/系统弹窗，或重启手机 USB 调试后再跑真机截图。
