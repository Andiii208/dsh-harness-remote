packages:
  - "apps/*"
  - "packages/*"
  - "mock-harness"
  - "harness-plugin"
  - "tools/*"
allowBuilds:
  # esbuild postinstall only validates the platform binary (present via
  # @esbuild/win32-x64); skip it — child-process pipe capture is restricted here.
  esbuild: false
